// find minimum Element of the array

let arr = [5, 3, 6, 7, 8, 9, 1, 2, 10];

// so to find minimum element... firs of all we have to sort the array
// and then print the first index element

// sorting the array

let sortedArray = arr.sort();

console.log("Minimum element = " + sortedArray[0]);
